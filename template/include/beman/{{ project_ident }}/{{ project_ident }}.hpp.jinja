// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
{% set identity = "identity" if generating_exemplar else "todo" %}

#ifndef BEMAN_{{project_ident.upper()}}_{{project_ident.upper()}}_HPP
#define BEMAN_{{project_ident.upper()}}_{{project_ident.upper()}}_HPP

#include <beman/{{project_ident}}/config.hpp>

#if BEMAN_{{project_ident.upper()}}_USE_MODULES() && !defined(BEMAN_{{project_ident.upper()}}_INCLUDED_FROM_INTERFACE_UNIT)

import beman.{{project_ident}};

#else

    #include <beman/{{project_ident}}/{{identity}}.hpp>

#endif // BEMAN_{{project_ident.upper()}}_USE_MODULES() &&
       // !defined(BEMAN_{{project_ident.upper()}}_INCLUDED_FROM_INTERFACE_UNIT)

#endif // BEMAN_{{project_ident.upper()}}_{{project_ident.upper()}}_HPP
